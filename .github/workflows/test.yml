name: test
on:
  push:
    branches:
      - main
jobs:
  test1:
    runs-on: ubuntu-latest
    steps:

      - name: test cal test2
        uses: ./.github/actions/test2@master
        with:
          toto: tatatatata
