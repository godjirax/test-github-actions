name: Jackpot API & dependencies push (env. dev/staging)
concurrency:
  group: jackpot-io/dev
  cancel-in-progress: false
on:
  push:
    branches:
      - main
jobs:
  test1j:
    runs-on: ubuntu-latest
    steps:
      - name: sleep 10
        run: |
          echo "will sleep"
          sleep 10
          echo "sleep finish"
